package org.example.listener;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

/**
 * @author: jinyun
 * @date: 2021/4/10
 */
@Slf4j
@Component
public class TestJobListener {

    //         jobDTO.setAppName("light-chain-customer");
    //            jobDTO.setExecuteTime(Date.from(customerEmphasisBasic.getOutBlacklistTime().atZone(ZoneId.systemDefault()).toInstant()));
    //            jobDTO.setJsonParam(param.getCustomerId().toString());
    //            jobDTO.setJobEnumCode(BaseJobEnum.OUTBAN_JOB.getCode());
    //            R<Integer> jobR = jobClient.createJob(jobDTO);
}
